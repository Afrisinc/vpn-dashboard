services:
  afrisinc-web:
    image: afrisinc-web:latest
    container_name: afrisinc-web-local
    ports:
      - "8090:8090"
    environment:
      - NODE_ENV=production
      - API_URL=${API_URL:-http://localhost:8091}
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8090"]
      interval: 30s
      timeout: 10s
      retries: 3
